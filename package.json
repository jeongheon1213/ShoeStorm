{
  "name": "shoestorm",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --port=3000 --open --out-dir start",
    "build": "parcel build index.html --out-dir bulidDist --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "examples": "^0.1.0",
    "parcel-bundler": "^1.12.5",
    "three": "^0.126.1",
    "three-orbit-controls": "^82.1.0"
  }
}
